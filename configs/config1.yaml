# Model
latent_dim: 64
attribute_embedding_dim: 32
block_size: 1024
d_model: 256
num_heads: 6
dropout: 0.1
num_layers: 4

# Training
batch_size: 8
learning_rate: 3.0e-4
seq_length: 1024
epochs: 10
num_training_iterations: 10000

# KL annealing
max_beta: 0.1
ratio_zero: 0.1
ratio_increase: 0.1
